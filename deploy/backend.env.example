# The exact frontend origin, without a trailing slash.
GALLERY_FRONTEND_ORIGIN=https://gallery.example.com
# Optional external reverse-proxy subpath (proxy strips it before forwarding).
GALLERY_PUBLIC_PREFIX=/model-gallery
